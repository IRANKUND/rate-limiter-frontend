# RateLimiterUi

## How to run frontent

```bash
npm run build
npm start
```

# After starting the frontend server

    Go to `http://localhost:4200` click on client add client id and client name
    Then add rules
    And click to dashboard start test late limit by sending notification
